// Re-export from SessionView as the single source of truth.
// This preserves the old import path during migration.
export { getSessionColor as sessionColorForKey } from "../components/sessions/SessionView.js";
